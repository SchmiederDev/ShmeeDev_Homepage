---
layout: base
pagination:
  data: student
  size: 1
  alias: student
permalink: "student-projects/{{ student.title | slugify }}/"
---

<div class="breakout">
{% include "includes/studentPageList.njk" %}
</div>

<h1>{{ student.title }}</h1>
<hr class="color-accent-tertiary breakout">


{% if student.image.src %}
{% image student.image.src, student.image.alt, "twi__img" %}
{% endif %}

{% if student.aboutGame %}
  <h2>About the game</h2>
  {% for item in student.aboutGame %}
  <p>{{item}}</p>
  {% endfor %}
{% endif %}

{% if student.story %}
  <h2>Story</h2>
  {% for item in student.story %}
  <p>{{item}}</p>
  {% endfor %}
{% endif %}


{% if student.video.videoId %}
  <h2>{{student.video.type}}</h2>
  <iframe width="600" height="338" src="https://www.youtube-nocookie.com/embed/{{student.video.videoId}}" title="{{student.video.description}}" frameborder="0" allow="encrypted-media;" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
{% endif %}


{% if student.responsibilities %}
  <h2>Responsibilities</h2>

  {% for item in student.responsibilities %}

    <div class="breakout two-column">
      <div class="two-column__one">
        <h3>{{item.role}}</h3>

      </div>
      <div class="two-column__two flow">
        {% for part in item.description %}
        <p>{{part}}</p>
        {% endfor %}
      </div>
    </div>

  {% endfor %}

{% endif %}


{% if student.gallery[0].src %}
  <section class="splide breakout flow" aria-labelledby="carousel-heading">
    <h2 id="carousel-heading">Project images</h2>

    <div class="splide__track">
      <ul class="splide__list">
        {% for item in student.gallery %}
        <li class="splide__slide">
          {% image item.src, item.alt, "gallery__img" %}
        </li>
        {% endfor %}
      </ul>
    </div>
  </section>
{% endif %}


<script>
  document.addEventListener( 'DOMContentLoaded', function() {
    var splide = new Splide( '.splide', 
    {
      type  : 'fade',
      rewind: true,
      padding: '3rem',
      gap:'2rem',
      lazyLoad: 'nearby',
    } );
    splide.mount();
  } );
</script>


<script defer src="/assets/vendor/splide-4.1.3/dist/js/splide.min.js"></script>
